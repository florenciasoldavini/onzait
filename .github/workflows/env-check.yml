name: CI Checks

on:
  pull_request:
  push:
    branches:
      - main
      - development

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install app dependencies
        run: npm ci

      - name: Documentation drift check (.env.example)
        run: npm run env:check

      - name: Expo dependency compatibility
        run: npm run doctor

      - name: TypeScript check
        run: npx tsc --noEmit

      - name: Lint
        run: npm run lint

  tests:
    name: Unit tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install app dependencies
        run: npm ci

      - name: Run unit tests
        run: npm test

  web_build:
    name: Web build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install app dependencies
        run: npm ci

      - name: Build production web app
        run: npm run build

      - name: Check web bundle budgets
        run: npm run bundle:check

  edge_functions:
    name: Edge Functions
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Deno
        uses: denoland/setup-deno@v2
        with:
          cache: true
          deno-version: v2.1.x

      - name: Verify Edge Functions
        run: npm run functions:verify

  database_tests:
    name: Database and RLS tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: 2.98.2

      - name: Start local Postgres
        run: supabase db start

      - name: Run database and RLS tests
        run: supabase test db

  ci-checks:
    name: ci-checks
    if: ${{ always() }}
    needs:
      - quality
      - tests
      - web_build
      - edge_functions
      - database_tests
    runs-on: ubuntu-latest

    steps:
      - name: Confirm all required jobs passed
        env:
          QUALITY_RESULT: ${{ needs.quality.result }}
          TESTS_RESULT: ${{ needs.tests.result }}
          WEB_BUILD_RESULT: ${{ needs.web_build.result }}
          EDGE_FUNCTIONS_RESULT: ${{ needs.edge_functions.result }}
          DATABASE_TESTS_RESULT: ${{ needs.database_tests.result }}
        run: |
          if [[ "$QUALITY_RESULT" != "success" || \
                "$TESTS_RESULT" != "success" || \
                "$WEB_BUILD_RESULT" != "success" || \
                "$EDGE_FUNCTIONS_RESULT" != "success" || \
                "$DATABASE_TESTS_RESULT" != "success" ]]; then
            echo "One or more required CI jobs did not pass."
            exit 1
          fi

          echo "All required CI jobs passed."
