'use client';
import { vars } from 'nativewind';
import { gluestackThemeVars } from '@/theme/tokens';

export const config = {
  light: vars(gluestackThemeVars.light),
  dark: vars(gluestackThemeVars.dark),
};
